# --------------------------------------------------
# HEALTH ASSISTANT UI
# --------------------------------------------------

st.subheader("🤖 MediNexa AI Health Assistant")

question = st.text_input(
    "Ask a question about your report"
)

if question:

    answer = health_assistant(
        question,
        report_data,
        abnormalities
    )

    st.info(answer)
